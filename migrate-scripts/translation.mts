

const getFiles = async (): Promise<string[]> => {
    /**
     * @todo Get all markdown files (`.md`) under currnt workspace.
     * @todo Recursion folders.
     * @todo The process should ignore stuff in .gitignore
     */

    return [];
}

console.log(await getFiles());
